# PostCraft CLI Commands

`node postcraft.js [COMMAND] [OPTIONS]`

With Bash shell script `pc`

If necessary :

`chmod +x pc`

Run as :

`./pc [COMMAND] [OPTIONS]`

## Prompt Commands

- `pc -s[=PORT]` : render and serve on port (default 8666)
